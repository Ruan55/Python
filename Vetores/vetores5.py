import os
os.system('cls || clear')

# Solicitando os dados do usuario
notas = []

for i in range(3):

    nota = float(input(f'Digite as notas: '))

# Exibindo os dados
for i in range(3):

    print(f'Notas: {nota}')